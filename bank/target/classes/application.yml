spring:
  application:
    name: bank
  sql:
    init:
      mode: always 
  datasource: 
    url: jdbc:mariadb://localhost:3306/department 
    driverClassName: org.mariadb.jdbc.Driver 
    username: root 
    password: 
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true 
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect 